{
  "name": "time_manager_assets",
  "version": "1.0.0",
  "description": "Assets for Time Manager",
  "scripts": {
    "deploy": "npm install && npm run deploy"
  },
  "dependencies": {
    "webpack": "^5.58.2",
    "webpack-cli": "^4.9.0"
  }
}
